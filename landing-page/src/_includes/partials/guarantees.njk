<section class="pain-points">
    <div class="container">
        <h2 class="section-title">Warum Sie uns vertrauen können</h2>
        <div style="margin-bottom: 2rem; text-align: center;">
            <div class="image-container" style="max-width: 800px; margin: 0 auto;">
                <img src="/assets/images/team-meeting.jpg" alt="Professionelle Planung und Organisation"
                     width="800" height="300" loading="lazy">
            </div>
        </div>

        <div class="pain-grid">
            {% for signal in pageContent.trustSignals %}
            <div class="pain-item">
                <div style="font-size: 2rem; margin-bottom: 0.5rem;">{{ signal.icon }}</div>
                <h3>{{ signal.title }}</h3>
                <p>{{ signal.description }}</p>
            </div>
            {% endfor %}
        </div>
    </div>
</section>
