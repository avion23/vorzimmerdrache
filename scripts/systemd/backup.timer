[Unit]
Description=Daily Vorzimmerdrache Backup Timer

[Timer]
# Run daily at 2:00 AM (low traffic time)
OnCalendar=*-*-* 02:00:00
Persistent=true

# Randomize by up to 30 minutes to avoid conflicts
RandomizedDelaySec=1800

[Install]
WantedBy=timers.target
